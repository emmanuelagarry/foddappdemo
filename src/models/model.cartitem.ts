export interface CartItem {
  price: string
  img: string
  name: string
  key?: number
}

// cordova plugin add ionic-plugin-deeplinks --variable URL_SCHEM
// E = agarry--variable DEEPLINK_SCHEME = https--variable DEEPLINK_HOST = emmanuel.com--variable ANDROID_PATH_PREFIX = /
